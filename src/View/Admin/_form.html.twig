{% if errors %}
<div class="errors">
    <h3>Erreur :</h3>
    {% for error in errors %}
    <p>{{ error }}</p>
    {% endfor %}
</div>
{% endif %}

<form class="formMedia" method="POST" enctype="multipart/form-data">
    {% if categorie %}
    <input type="hidden" name="id" id="id" value={{ categorie.id }}>
    <label for="name">Nom :</label>
    <input type="text" id="name" name="name" value="{{ categorie.name }}">
    <input type="submit" value="{{ isEdit ? " Modifier" : "Ajouter" }} la catégorie">


    {% else %}
    <input type="hidden" name="id" id="id" value={{ type.id }}>
    <label for="name">Nom :</label>
    <input type="text" id="name" name="name" value="{{ type.name }}">
    <input type="submit" value="{{ isEdit ? " Modifier" : "Ajouter" }} le type">
    {% endif %}
</form>